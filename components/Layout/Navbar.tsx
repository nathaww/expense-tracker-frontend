const Navbar = () => {
  return (
    <nav className="flex justify-between items-center p-4 bg-[var(--bg)] border-b border-[var(--border-color)] text-white">
      <div className="text-lg font-bold text-[var(--text)]">Expense Tracker</div>
    </nav>
  );
};

export default Navbar;
